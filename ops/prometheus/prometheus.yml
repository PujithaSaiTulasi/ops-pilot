# Prometheus scrape configuration for the local OpsPilot stack.
global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: opspilot-api
    metrics_path: /metrics
    static_configs:
      - targets: ["api:8000"]
        labels:
          service: opspilot
