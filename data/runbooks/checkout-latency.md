# Checkout latency after a deployment

If checkout latency rises immediately after a deployment, compare the active
version with the previous healthy version. Inspect checkout traces and the
changed files. A rollback is a side effect and requires human approval.
